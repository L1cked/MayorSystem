# Admin Perk Commands

```text
/%title_command% admin perks
/%title_command% admin perks refresh
/%title_command% admin perks refresh <player|--all|all>
/%title_command% admin perks requests
/%title_command% admin perks requests approve <id>
/%title_command% admin perks requests deny <id>
/%title_command% admin customperk <id> <approve|deny>
/%title_command% admin perks catalog
/%title_command% admin perks catalog section <section> <toggle|on|off>
/%title_command% admin perks catalog perk <section> <perk> <toggle|on|off>
```

Fallback command root: `/mayor`.

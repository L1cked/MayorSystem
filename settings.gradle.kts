plugins {
    // Auto-download the correct JDK if you don't have it installed (fixes the "NoToolchainAvailable" error).
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "MayorSystem"
